# 💓 心跳机制调试指南

## 🔧 已优化的问题

### 1️⃣ **修复了 Pong 监听器重复注册**
**问题：** 原代码在每次发送 Ping 时都会注册新的 Pong 监听器，导致内存泄漏。

**修复：** 将 Pong 监听器移到连接建立时只注册一次。

```javascript
// 优化前：每次 ping 都注册新监听器 ❌
setInterval(() => {
  this.ws.ping();
  this.ws.on('pong', () => { ... }); // 重复注册！
}, 30000);

// 优化后：只注册一次 ✅
setupPongListener() {
  this.ws.on('pong', () => { ... }); // 只注册一次
}
```

### 2️⃣ **添加详细的时间戳日志**
所有心跳相关日志都带有时间戳，方便追踪。

### 3️⃣ **添加异常处理**
Ping 发送失败时会捕获并记录错误。

### 4️⃣ **添加连接状态检查**
在发送 Ping 前检查 WebSocket 状态。

## 📊 日志输出说明

### 启动阶段
```
💓 启动心跳机制（30秒间隔）
```

### 正常心跳
```
💓 [14:20:00] 发送 Ping...
💓 [14:20:00] 收到 Pong 响应
```

### Pong 超时
```
💓 [14:20:30] 发送 Ping...
⚠️ [14:20:40] Pong 超时（10秒无响应），强制重连...
🔌 [14:20:40] WebSocket 连接关闭 (code: 1006, reason: 无)
💓 停止心跳机制
⏰ [14:20:40] 5 秒后尝试重连...
```

### 连接异常
```
⚠️ WebSocket 状态异常，无法发送 Ping (state: 2)
```

**WebSocket 状态码：**
- `0` = CONNECTING（连接中）
- `1` = OPEN（已连接）
- `2` = CLOSING（关闭中）
- `3` = CLOSED（已关闭）

### 发送失败
```
❌ 发送 Ping 失败: Connection closed
```

### 停止心跳
```
💓 停止心跳机制
```

## 🔍 监控要点

### 1. **正常情况**
每30秒应该看到：
```
💓 [时间] 发送 Ping...
💓 [时间] 收到 Pong 响应
```

### 2. **网络延迟**
如果 Pong 响应时间超过1-2秒，可能存在网络延迟。

### 3. **连接不稳定**
频繁出现以下情况说明连接不稳定：
```
⚠️ Pong 超时（10秒无响应），强制重连...
```

### 4. **服务器问题**
如果持续无法收到 Pong，可能是：
- Hyperliquid 服务器问题
- 防火墙阻止 WebSocket
- 网络连接中断

## 🛠️ 调试建议

### 查看完整心跳日志
```bash
npm start | grep "💓"
```

### 查看所有 WebSocket 事件
```bash
npm start | grep -E "(🔌|💓|⚠️|❌)"
```

### 统计心跳频率
观察日志中 "发送 Ping" 的时间间隔是否为30秒。

### 检查 Pong 响应率
统计 Ping 和 Pong 的数量比例：
- 正常：1:1（每个 Ping 都有对应的 Pong）
- 异常：Ping 多于 Pong（网络问题）

## 📈 性能指标

### 健康状态
- ✅ Ping/Pong 比例 = 1:1
- ✅ Pong 响应时间 < 1秒
- ✅ 30秒内无超时
- ✅ 无频繁重连

### 需要关注
- ⚠️ Pong 响应时间 > 2秒
- ⚠️ 偶尔出现超时（< 5次/小时）
- ⚠️ 重连间隔 > 5分钟

### 严重问题
- ❌ Ping/Pong 比例 < 0.5
- ❌ 频繁超时（> 10次/小时）
- ❌ 持续无法连接
- ❌ 重连间隔 < 1分钟

## 🔄 重启应用

代码已更新，重启服务查看新日志：

```bash
npm start
```

## 📝 日志示例

### 完整的心跳周期
```
✅ WebSocket 连接成功
💓 启动心跳机制（30秒间隔）
🔄 重新订阅 1 个地址...
✅ 已订阅地址: 0xb317...83ae

[30秒后]
💓 [14:20:00] 发送 Ping...
💓 [14:20:00] 收到 Pong 响应

[30秒后]
💓 [14:20:30] 发送 Ping...
💓 [14:20:30] 收到 Pong 响应

[持续运行...]
```

## 🎯 优化效果

### 优化前
- ❌ 内存泄漏（监听器重复注册）
- ❌ 难以调试（缺少日志）
- ❌ 无异常处理

### 优化后
- ✅ 无内存泄漏
- ✅ 详细的时间戳日志
- ✅ 完善的异常处理
- ✅ 状态检查机制

现在你可以清楚地看到心跳机制的运行状态！🚀
