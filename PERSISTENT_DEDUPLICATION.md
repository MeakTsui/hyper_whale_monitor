# 🗄️ 持久化去重机制

## 📊 问题背景

### 之前的问题
- ✅ 内存级别去重 - 运行时有效
- ❌ 重启后失效 - 历史数据重复发送
- ❌ 无法跨会话 - 每次启动都是新的

### 现在的解决方案
- ✅ 数据库持久化 - 重启后仍然有效
- ✅ 两级缓存 - 内存 + 数据库
- ✅ 自动清理 - 定期清理过期数据

## 🏗️ 架构设计

### 两级缓存机制

```
┌─────────────────────────────────────┐
│         新交易到达                    │
└──────────────┬──────────────────────┘
               │
               ▼
┌─────────────────────────────────────┐
│   第一级：内存缓存检查                 │
│   Map<tradeId, timestamp>           │
│   - 速度快（毫秒级）                   │
│   - 容量有限                          │
│   - 重启后清空                        │
└──────────────┬──────────────────────┘
               │ 未找到
               ▼
┌─────────────────────────────────────┐
│   第二级：数据库检查                   │
│   SQLite processed_trades 表         │
│   - 持久化存储                        │
│   - 重启后保留                        │
│   - 容量大                           │
└──────────────┬──────────────────────┘
               │ 未找到
               ▼
┌─────────────────────────────────────┐
│   处理新交易                          │
│   1. 发送通知                        │
│   2. 记录到内存                      │
│   3. 持久化到数据库                  │
└─────────────────────────────────────┘
```

## 📋 数据库表结构

### processed_trades 表

```sql
CREATE TABLE processed_trades (
  id INTEGER PRIMARY KEY AUTOINCREMENT,
  trade_id TEXT UNIQUE NOT NULL,      -- 交易唯一ID
  address TEXT NOT NULL,               -- 钱包地址
  coin TEXT NOT NULL,                  -- 币种
  timestamp INTEGER NOT NULL,          -- 交易时间戳
  created_at DATETIME DEFAULT CURRENT_TIMESTAMP  -- 记录创建时间
);

-- 索引
CREATE INDEX idx_trade_id ON processed_trades(trade_id);
CREATE INDEX idx_timestamp ON processed_trades(timestamp);
```

### 交易ID生成规则

```javascript
tradeId = `${address}_${coin}_${timestamp}_${price}_${size}`

// 示例
"0xb317...83ae_BTC_1761182935279_108150.0_0.0001"
```

## 🔄 工作流程

### 1. 启动时
```
1. 初始化数据库
2. 创建/验证表结构
3. 内存缓存为空
4. 数据库中保留历史记录
```

### 2. 收到交易时
```
1. 生成 tradeId
2. 检查内存缓存 → 命中则跳过
3. 检查数据库 → 命中则跳过（并加入内存）
4. 都未命中 → 处理新交易
5. 记录到内存 + 数据库
```

### 3. 重启后
```
1. 内存缓存清空
2. 数据库保留所有记录
3. 历史交易会被数据库拦截
4. 只有新交易才会被处理
```

## 📊 日志输出

### 内存命中
```
⏭️  跳过重复交易(内存): 0xb317...83ae_BTC_1761182935279_108150.0_0.0001...
```

### 数据库命中
```
⏭️  跳过重复交易(数据库): 0xb317...83ae_BTC_1761182935279_108150.0_0.0001...
```

### 清理日志
```
🧹 清理了 150 条内存中的过期交易记录
🧹 清理了 5000 条数据库中的过期交易记录（>7天）
📊 交易记录统计: 总计 12345 条, 最早 2025/10/23 10:30:00, 最新 2025/10/30 14:50:00
```

## 🧹 自动清理机制

### 内存清理
- **频率**: 每 10 分钟
- **规则**: 删除 1 小时前的记录
- **目的**: 防止内存占用过大

### 数据库清理
- **频率**: 每 24 小时
- **规则**: 删除 7 天前的记录
- **目的**: 保持数据库大小合理

### 手动清理
```javascript
// 清理 7 天前的记录
db.cleanOldTrades(7);

// 查看统计
const stats = db.getTradeStats();
console.log(stats);
// { total: 12345, oldest: '2025/10/23...', newest: '2025/10/30...' }
```

## 📈 性能优化

### 1. 内存优先
- 内存查询 < 1ms
- 大部分请求在内存层拦截
- 减少数据库访问

### 2. 索引优化
- `trade_id` 索引 - 快速查找
- `timestamp` 索引 - 快速清理

### 3. 批量操作
```javascript
// 支持批量插入
db.addProcessedTrades([
  { tradeId: '...', address: '...', coin: '...', timestamp: ... },
  { tradeId: '...', address: '...', coin: '...', timestamp: ... },
  // ...
]);
```

## 💾 数据库文件

### 位置
```
./data/monitor.db
./data/monitor.db-shm  (共享内存)
./data/monitor.db-wal  (预写日志)
```

### 备份建议
```bash
# 定期备份数据库
cp ./data/monitor.db ./backups/monitor_$(date +%Y%m%d).db

# 恢复
cp ./backups/monitor_20251030.db ./data/monitor.db
```

## 🔍 查询示例

### 检查特定交易
```javascript
const tradeId = "0xb317...83ae_BTC_1761182935279_108150.0_0.0001";
const exists = db.isTradeProcessed(tradeId);
console.log(exists); // true/false
```

### 查看统计
```javascript
const stats = db.getTradeStats();
console.log(`总记录: ${stats.total}`);
console.log(`最早: ${stats.oldest}`);
console.log(`最新: ${stats.newest}`);
```

### 清理旧数据
```javascript
// 清理 3 天前的数据
const deleted = db.cleanOldTrades(3);
console.log(`删除了 ${deleted} 条记录`);
```

## 🎯 使用场景

### 场景 1: 正常运行
```
交易 A → 内存未找到 → 数据库未找到 → 处理 ✅
交易 A → 内存找到 → 跳过 ⏭️
```

### 场景 2: 重启后
```
交易 A (历史) → 内存未找到 → 数据库找到 → 跳过 ⏭️
交易 B (新的) → 内存未找到 → 数据库未找到 → 处理 ✅
```

### 场景 3: WebSocket 重连
```
收到历史数据 → 数据库拦截 → 全部跳过 ⏭️
收到新数据 → 正常处理 ✅
```

## 📊 效果对比

### 优化前（仅内存）
```
启动 → 收到 30 笔历史交易 → 发送 30 条通知 ❌
重启 → 收到 30 笔历史交易 → 发送 30 条通知 ❌
```

### 优化后（内存 + 数据库）
```
启动 → 收到 30 笔历史交易 → 发送 30 条通知 ✅
重启 → 收到 30 笔历史交易 → 全部跳过 ✅
重启 → 收到 5 笔新交易 → 发送 5 条通知 ✅
```

## 🚀 启动应用

```bash
npm start
```

### 首次启动
```
📦 初始化数据库...
✅ 数据库表初始化完成
🔍 初始化 Hyperliquid 监控器...
```

### 重启后
```
📦 初始化数据库...
✅ 数据库表初始化完成
📊 交易记录统计: 总计 12345 条, 最早 2025/10/23...
```

## 💡 最佳实践

### 1. 定期备份
- 每天备份数据库文件
- 保留最近 7 天的备份

### 2. 监控大小
- 定期检查数据库大小
- 调整清理策略（默认 7 天）

### 3. 性能调优
- 保持内存缓存有效（1小时）
- 数据库不要保留太久（7天足够）

### 4. 故障恢复
- 数据库损坏 → 从备份恢复
- 数据丢失 → 会重复发送一次通知（可接受）

## 🎉 总结

通过**两级缓存**机制：
- ✅ 运行时高性能（内存）
- ✅ 重启后不重复（数据库）
- ✅ 自动清理过期数据
- ✅ 完美解决历史数据重复问题

现在无论何时重启，都不会再收到重复的历史交易通知！🚀
